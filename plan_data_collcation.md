# 数据采集改进计划

## 目标

- 三路 RGB 以 640×480、30 Hz 采集，主相机深度以 640×480、30 Hz、uint16 无损采集。
- 双臂、双手状态及已成功提交的控制目标完整记录，默认低维状态频率 200 Hz。
- 录制、编码、压缩和磁盘抖动不得阻塞运动控制。
- 正式数据不得静默丢失；发生缺失时明确作废当前条，但不因单纯采集故障停止遥操作。
- 在线阶段只做必要采集与顺序写入，正运动学、Zarr 压缩、对齐和质检离线完成。
- 最终仍生成 `episode.json`、三路 MP4 和 `raw.zarr`，保持现有转换程序兼容。

## 目标架构

```text
Quest ───────────────> 运动控制主进程 ─────> 天机双臂
                             │
Wuji设备 ────────────> Wuji进程 ──────────> 双手
                             │
设备反馈和已提交目标          └──> 低维共享内存队列 ──> 录制协调进程顺序写入

RealSense 0 ─> 独立采集线程 ─> RGB正式帧池 ─> 相机0 NVENC编码
                             ├> 深度正式帧池 ─> 深度写入进程
                             └> 预览最新帧共享区
RealSense 1 ─> 独立采集线程 ─> RGB正式帧池 ─> 相机1 NVENC编码
                             └> 预览最新帧共享区
RealSense 2 ─> 独立采集线程 ─> RGB正式帧池 ─> 相机2 NVENC编码
                             └> 预览最新帧共享区

原始会话 ──> 显式离线整理 ──> episode.json + camera_*.mp4 + raw.zarr
```

## 实施内容

### 1. 控制链路隔离

- 设备适配器通过非阻塞录制旁路发布实测反馈和已成功提交的控制目标。
- 旁路只复制固定尺寸数值与时间戳，不执行图像处理、正运动学、压缩或磁盘操作。
- 默认将运动控制和 Wuji 放在逻辑核 `0,4,1,5`，采集、编码、写入和预览放在 `2,6,3,7`；拓扑不匹配时自动停用亲和性而不是猜测核编号。
- 采集故障结束并作废当前条，但不触发运动暂停；设备故障和安全看门狗仍按现有逻辑处理。

### 2. 低维数据共享内存

- 每条数据流使用独立的固定形状共享内存数组，保存 `time_ns`、`sequence` 和对应数值字段。
- 使用单调写入/读取序号形成有界环形队列；只有已经持久化的槽位才能复用，禁止覆盖未消费记录。
- 缓冲区满时不等待控制线程，立即标记当前条失败并记录流名、生产序号、消费序号和容量。
- 每条流提供至少约 10 秒容量；离线整理只有在生产数、消费数、磁盘数相同且源序号严格递增时才发布完整条目。设备自身产生的源序号间隔予以保留，不伪造补帧。

### 3. 相机、编码与预览

- 一个隔离于运动控制的录制进程持有三台 RealSense，每台相机使用独立采集线程；三路 RGB 分别进入独立共享帧池。相机线程只取帧、校验和复制，不承担编码或压缩。
- 每路 RGB 由独立 NVENC 编码单元处理；正式模式不自动退回三路 CPU 编码。
- 主相机深度由独立写入单元顺序保存为无压缩分段文件，离线再压缩为 Zarr。
- 每台相机另设一块 5 Hz 最新 RGB 预览共享区。预览允许覆盖旧预览帧，但不读取、不锁定正式录制帧池。
- 正式帧保留设备序号、设备时间和主机单调时间，禁止覆盖未编码或未写入的数据。

### 4. 原始会话与离线整理

- 在线阶段保存三路临时视频、主相机无损深度分段、低维分段和原始会话清单。
- 增加唯一必要入口：

  ```bash
  python scripts/finalize_recording.py --input recordings/<session>
  ```

- 离线整理校验计数与序号、计算正运动学、压缩深度和低维数据、封装最终 MP4，并原子更新 `episode.json`。
- 条目状态为 `capturing`、`captured`、`finalizing`、`complete`、`failed`、`discarded`；只有 `complete` 可进入现有 `convert_recording.py`。

### 5. NVENC 前置检查

- 启用 `--record` 时，在机械臂运动前检查 NVIDIA 驱动、编码器创建、三路并发编码和解码计数。
- 检查失败时拒绝进入录制模式；不影响未启用 `--record` 的普通遥操作。

### 6. 快速使用说明

- 新增 `docs/recording_quickstart.md`，只包含硬件检查、环境启动、按键流程、离线整理、数据转换、条目状态和常见恢复步骤。
- 项目说明只链接快速说明，不复制完整采集文档。

## 实施顺序

1. 补齐资源遥测并隔离控制与采集故障。
2. 实施处理器亲和性，先通过未录制状态下的稳定性验证。
3. 将低维数据改为固定结构共享内存队列。
4. 建立三条独立相机采集线程、正式帧池、预览共享区和独立编码/深度写入。
5. 实现原始会话和离线整理。
6. 删除旧的串行编码、在线正运动学和在线 Zarr 压缩路径。
7. 更新快速使用说明并完成完整验收。

## 验收标准

- `--record --viewer` 接合但不按 C，连续 15 分钟无机械臂看门狗、Wuji 数据过期和控制暂停。
- 三路 RGB 30 Hz、一路深度 30 Hz、低维状态 200 Hz 连续录制 30 分钟，采集链路无内部丢弃；设备源数据缺口被准确记录并在转换时切分。
- 预览停止或卡顿不影响正式采集。
- 连续完成至少 100 次开始、保存、作废和重新开始。
- 编码、深度或低维写入进程失败时，当前条失败但遥操作继续。
- 异常退出后离线整理不会发布伪完整数据。
- 最终结果可由现有 `scripts/convert_recording.py` 直接读取。

## 精简约束

- 不新增第二个遥操作入口，不新增每相机启动脚本。
- 不并存新旧两套正式录制通道，不复制现有数据转换逻辑。
- 只新增一个离线整理入口和一份快速使用说明。
- 所有采集子进程均由现有遥操作程序统一创建、停止和回收。
